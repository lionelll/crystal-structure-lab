# 晶体结构项目开发补充规则

三个材料实验项目的共同开发、分支、授权、发布与回滚规则统一以 `公共文档/01-架构与开发/三项目统一开发规范.md` 为准。本文件只记录晶体结构项目的附加要求，不得改变公共按版本开发的分支模型。

## 分支职责

- `feature/vMAJOR.MINOR.PATCH`：一个版本对应一个开发分支，该版本全部代码、配置、文档、测试和修复均在此完成；版本号及实际分支名必须由用户确认。
- `dev`：只合并本次待发布版本 `feature` 并进行集成验证，不直接编写或修复代码、配置、测试和开发文档。
- `release`：只接收验证通过且获准发布的 `dev`，禁止直接修改。
- `main`：只备份已经正式部署并验收的 `release`，不开发、不部署。
- 版本分支从已同步且基线明确的最新 `dev` 创建；后续版本不提前合入当前待发布 `dev`。未经专项授权不使用 `fix/*`、`hotfix/*` 或其他开发分支，紧急修复不得绕过验收与发布流程。

## v1.2.0 本次执行范围

- 用户已授权版本 `v1.2.0`、分支 `feature/v1.2.0` 和晶体结构工作区内 `.worktrees/v1.2.0/` 附属工作树；按工作区 `文档/04-版本记录/v1.2.0-开发授权与工作树说明.md` 创建并进行本地开发、测试、构建和浏览器验证。
- 工作树属于原仓库，原 `dev` 工作树的未提交文档不需要先提交或上传 GitHub。允许把已移交的两份最新规范应用到新工作树，保留原文件不变；新工作树中的已知开发修改可以保持未提交状态继续测试。
- 主工作树读取 `../AGENTS.md`，`.worktrees/v1.2.0/` 读取 `../../AGENTS.md` 作为晶体结构工作区规则；公共规范相对材料项目根解析。
- 不重复申请本次已登记的版本、路径和初始化权限。暂存开发修改、提交、推送、合并、Tag、服务器部署和清理仍未授权。

## 开发前检查

1. 确认命令目录和 Git common-dir 属于本仓库；本次开发目录为已登记的 `.worktrees/v1.2.0/`，不要在保留的原 `dev` 工作树开发。
2. 开发修改时确认当前分支为用户批准的版本 `feature`，核对版本范围和创建基线；只读检查、集成、验证和发布准备不代表获得在 `dev`、`release`、`main` 直接修改的权限。
3. 检查 `git status`，来源不明的修改不得覆盖、删除或代为提交。
4. 拉取、合并、变基或切换分支前保持工作树干净；不为清空工作树擅自提交、暂存、隐藏或还原现有内容。开发提交只纳入本任务获准文件；推送已经创建并核验哈希的明确分支或 Tag，继续按公共规范的“已核验引用”例外执行。
5. 同一工作树同时只允许一个写任务。已移交的治理文档按公共开发规范第九节纳入获准版本 `feature`，不直接在 `dev` 提交。
6. 创建分支、修改、提交、推送、合并、Tag、部署和删除分支均需对应授权；允许用户一次列明多个动作，不相互推定。

## 项目验证

在版本 `feature` 开发、编写测试和功能自测；提交前至少运行：

```text
npm test
npm run build
```

涉及页面、三维模型或交互的修改，还必须完成本地浏览器冒烟测试，检查核心功能、刷新、静态资源、控制台和受影响的桌面端/移动端布局。

## 测试环境

- 登记的共享测试服务器地址为 `http://123.57.11.145:8080/crystal/`，服务器部署来源仍只能是 `dev`；版本 `feature` 的本地功能自测不等于获准直接部署到该服务器。
- 推送 `dev` 如果会触发自动测试部署，推送前必须明确说明这一外部影响并取得授权。
- 项目负责人可以在当次任务明确授权后自行部署测试环境，唯一允许写入的测试目录为 `/var/www/crystal-dev`。
- 测试部署必须遵守公共的 `三项目测试环境部署规范.md`，不得修改公共 Nginx、系统配置或其他项目目录。
- 测试通过不代表获得合并 `release`、打 Tag 或正式发布授权。

## 正式发布资料

- 目标正式地址为 `https://lab.changyanedu.cn/crystal/`。
- 版本 `feature` 测试通过后，经授权快进合入 `dev`；如需同步最新 `dev`，先在版本分支完成并解决冲突、复测，不在 `dev` 手改。
- 合并后的 `dev` 必须重新完成项目测试、构建和浏览器集成验证；问题回对应版本 `feature` 修复后重新集成。验收记录绑定项目、版本、完整提交号及产物校验信息。
- `release` 只能快进接收经批准且通过集成验收的 `dev` 精确提交，不得混入之后的新提交；快进失败时停止，不得在 `release` 解决冲突。
- 正式 Tag 使用 `vMAJOR.MINOR.PATCH`，只能指向 `release` 的精确提交且不得移动。
- 项目负责人完成代码和产物准备后，将 Tag、提交号、产物、自测结果及回滚建议移交“10-部署发布”。
- 正式部署成功并验收后，只有再次获得授权才能把 `release` 快进同步到 `main` 作为备份。

## 禁止提交

- 密码、Token、私钥、证书、云服务器凭据和本机绝对路径配置。
- `.claude/`、个人编辑器配置、临时文件、日志、缓存和无关截图。
- `node_modules`、未经约定的构建产物及与当前任务无关的修改。
