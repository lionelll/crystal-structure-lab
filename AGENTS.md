# 晶体结构仓库强制规则

任何代码或 Git 操作开始前，必须按顺序完整阅读：

1. 材料项目根目录 `AGENTS.md`。
2. `公共文档/01-架构与开发/三项目统一开发规范.md`。
3. `公共文档/01-架构与开发/三项目统一代码架构规范.md`。
4. `公共文档/01-架构与开发/三项目测试环境部署规范.md`。
5. `公共文档/02-设计与验收/统一Design-QA规范.md`。
6. 晶体结构工作区 `AGENTS.md`：主工作树使用 `../AGENTS.md`，已授权 `.worktrees/v1.2.0/` 使用 `../../AGENTS.md`。
7. 当前工作树中的 `DEVELOPMENT.md`。
8. 晶体结构工作区的 `文档/04-版本记录/v1.2.0-开发授权与工作树说明.md`；根目录与公共规范路径相对材料项目根解析，不依赖当前命令目录猜测。

## 强制边界

- 本仓库按版本开发：一个用户确认的 `feature/vMAJOR.MINOR.PATCH` 分支承接该版本全部功能、修复、配置、测试及配套文档，不按每个小功能拆分；版本号与实际分支名不得自行决定。
- `dev` 只集成经授权的本次待发布版本 `feature` 并验证合并结果，禁止直接编写代码、修复问题或补写开发文档。集成发现问题回对应版本 `feature` 修复并复测，再经授权合入。
- `release` 只能接收经批准的 `dev`，禁止在 `release` 直接编辑或提交。
- `main` 只备份已正式发布并验收的 `release`，禁止开发和部署。
- 版本 `feature` 从已同步且基线明确的最新 `dev` 创建；`dev` 不得提前混入其他版本。未经专项授权不使用 `fix/*`、`hotfix/*` 或其他分支模型，历史分支不自动改名或删除。
- Git 写操作仅限本仓库及明确获准的同仓库附属工作树；本次为晶体结构工作区内 `.worktrees/v1.2.0/`。其他材料项目目录和 `10-部署发布/` 只读。
- 同一工作树同一时间只允许一个写任务；不同分支不构成文件隔离。

## 操作与验证

- 开发修改前检查当前目录、当前分支和 `git status`；不在用户确认的版本 `feature` 或存在来源不明修改时立即停止开发写入。`dev`、`release`、`main` 只允许各自获准的集成、验证、发布准备或备份操作。
- 经“00-架构师”明确移交的本仓库治理文档属于来源已确认、可处理的项目修改，由本项目负责人按授权在对应版本 `feature` 审阅并单独提交；不得夹带进无关代码提交。本次规则切换按公共开发规范第九节处理，不在 `dev` 直接提交，也不得擅自清理已有未提交内容。
- 工作树存在已知未提交文档时，可以按公共规范推送哈希已核验的现有分支或 Tag；不得借此合并、切换分支、移动 Tag 或创建新提交。
- 提交前至少运行 `npm test` 和 `npm run build`，并完成受影响页面的本地浏览器验证。
- 不得删除测试、放宽断言、隐藏错误或夹带无关修改。
- 版本 `feature` 测试通过后经授权合入 `dev`，合并后的精确提交必须重新执行项目测试、构建和浏览器集成验证；不能沿用合并前的验收结果。
- 合入前如需同步最新 `dev`，经授权在版本 `feature` 内完成并解决冲突、重新验证；禁止在 `dev`、`release`、`main` 手改冲突。具体快进门禁按公共开发规范执行。
- 创建分支、修改、暂存及提交、推送、合并、打 Tag、部署、同步 `main` 和删除分支分别需要明确授权；可在一次授权中逐项列明，不得相互推定。

## v1.2.0 开工授权

- 用户已授权本仓库负责人创建 `feature/v1.2.0` 和晶体结构工作区内 `.worktrees/v1.2.0/` 附属工作树，完成本次已确认需求的开发与本地验证；具体基线核对、必要 Git 元数据和文件权限以工作区中的开发授权说明为准。
- 允许在新工作树应用主工作树中已经移交的最新版 `AGENTS.md`、`DEVELOPMENT.md`，并读取正确的父级与公共规范；主工作树中的原文件、暂存区及分支保持不变。
- 本地开发和测试不以提交或推送 GitHub 为前提。本任务已知未提交修改可以继续开发、自测，不要求开发工作树一直干净，也不重复申请已登记的版本与目录权限。
- 本次不授权暂存开发修改、提交、推送、合并、变基、Tag、服务器部署或分支/工作树清理；这些动作另行授权。

## 部署移交

- 本仓库对话可以在当次任务明确授权后，把可追溯的 `dev` 版本部署到测试服务器的 `/var/www/crystal-dev`，不得写入其他服务器目录。
- 不得修改测试服务器公共 Nginx、系统配置、DNS、证书，也不得连接或修改正式服务器。
- 正式部署、正式回滚和共享基础设施问题统一移交“10-部署发布”。
- 发布资料必须包含版本、Tag、完整提交号、构建产物、自测结果、已知问题和回滚建议。
